# Intentionally empty.
#
# Mounted in the integrated deployment over the stock
# nginx:alpine /etc/nginx/conf.d/default.conf so that file's
# `listen 80;` server block doesn't load. VAS's real server block
# is generated from /etc/nginx/templates/vas.conf.template at
# startup and listens only on VAS_HTTP_PORT.
